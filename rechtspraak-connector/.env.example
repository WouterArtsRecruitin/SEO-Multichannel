# Copy to .env and fill in. Never commit the real .env.
# Only needed when export.target includes "supabase".
DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT.supabase.co:5432/postgres

# Optional: incoming webhook (Slack/Teams) for withdrawn-ECLI notifications.
NOTIFY_WEBHOOK_URL=
